import React from 'react';
import DynamicProperty from '../components/DynamicProperty';

const Detail = ({ pageContext }) => {
  return <DynamicProperty pageContext={pageContext} />;
};

export default Detail;
